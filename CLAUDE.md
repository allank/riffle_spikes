## Agent skills

### Issue tracker

Issues live in GitHub Issues for this repo (`allank/riffle_spikes`), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root. `docs/adr/` intentionally stays empty — this is a spike/investigation repo; consequential findings graduate into ADRs in `riffle` instead (see ADR-0002's addendum for the established pattern), not into an ADR trail here. See `docs/agents/domain.md`.
